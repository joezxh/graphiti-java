#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../vue-demi/bin/vue-demi-switch.js" "$@"
else
  exec node  "$basedir/../../../vue-demi/bin/vue-demi-switch.js" "$@"
fi
# cmd-shim-target=D:/projects/graphiti-java/ontograph-web/node_modules/.pnpm/vue-echarts@7.0.3_@vue+runt_6abcdd3ceb5a5b1feec2e9e1d78752d2/node_modules/vue-demi/bin/vue-demi-switch.js
