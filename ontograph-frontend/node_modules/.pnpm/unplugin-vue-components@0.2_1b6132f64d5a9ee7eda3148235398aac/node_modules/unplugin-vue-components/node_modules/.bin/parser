#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../@babel/parser/bin/babel-parser.js" "$@"
else
  exec node  "$basedir/../../../@babel/parser/bin/babel-parser.js" "$@"
fi
# cmd-shim-target=D:/projects/graphiti-java/ontograph-web/node_modules/.pnpm/unplugin-vue-components@0.2_1b6132f64d5a9ee7eda3148235398aac/node_modules/@babel/parser/bin/babel-parser.js
