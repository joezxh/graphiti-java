<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.graphiti</groupId>
        <artifactId>graphiti-java</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>
    <artifactId>graphiti-module-core</artifactId>
    <name>Graphiti Core Module</name>
    <description>核心业务模块：图谱管理、本体管理、检索服务、数据导入等</description>
    <packaging>jar</packaging>
    
    <dependencies>
        <!-- 系统模块 -->
        <dependency>
            <groupId>com.graphiti</groupId>
            <artifactId>graphiti-module-system</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- Web -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <!-- MyBatis -->
        <dependency>
            <groupId>com.graphiti</groupId>
            <artifactId>graphiti-spring-boot-starter-mybatis</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- Redis -->
        <dependency>
            <groupId>com.graphiti</groupId>
            <artifactId>graphiti-spring-boot-starter-redis</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- Neo4j Driver -->
        <dependency>
            <groupId>org.neo4j.driver</groupId>
            <artifactId>neo4j-java-driver</artifactId>
        </dependency>
        <!-- Spring AI -->
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-starter-model-openai</artifactId>
            <version>1.1.2</version>
        </dependency>
        <!-- JSON -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.datatype</groupId>
            <artifactId>jackson-datatype-jsr310</artifactId>
        </dependency>
        <!-- 工具 -->
        <dependency>
            <groupId>cn.hutool</groupId>
            <artifactId>hutool-all</artifactId>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>
        <!-- 测试 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
