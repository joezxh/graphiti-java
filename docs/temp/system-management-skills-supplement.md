# 4.5 系统管理模块 - Skill提示词补充内容

> 以下内容需要插入到 graphiti-test.md 的对应位置

---

## 4.5.1 用户管理 - 补充内容

在 `##### 预期测试时间: 15 分钟` 之后插入:

```markdown
##### Skill 配置

**引用的 Skill**:
- `browse` - 页面加载验证
- `qa` - 完整功能测试
- `open-gstack-browser` - 交互式用户管理测试

**前置条件**:
- ✅ 后端服务运行中
- ✅ 前端服务运行中
- ✅ 用户已登录 (admin)
- ✅ 数据库存在测试角色
- ✅ 存在至少 1 个测试用户

##### Skill 提示词: 用户管理测试

**页面路径**: `/system/user`  
**源码文件**: `src/views/system/UserManagement.vue`  
**组件依赖**: `@/components/user/UserForm.vue`, `UserTable.vue`, `RoleSelector.vue`  
**API 文件**: `@/api/user.ts` - `getUsers()`, `createUser()`, `updateUser()`, `deleteUser()`, `toggleUser()`, `resetPassword()`

```prompt
/browser 或 /open-gstack-browser
打开 OntoGraph 系统用户管理页面,执行完整的用户管理功能测试。

【前置操作】
1. 使用 admin/admin123 登录系统
2. 导航到 http://localhost:5173/system/user
3. 等待页面加载完成(最多 5 秒)

---

【测试场景 1:页面加载与用户列表】
验证用户管理页面的基础渲染和列表展示:

1. 页面标题验证:
   - 验证页面标题显示「用户管理」或 i18n 对应键
   - 验证页面描述文字存在

2. 顶部操作区验证:
   - 验证「新增用户」按钮存在,类型为 primary
   - 验证搜索框存在,placeholder 提示输入用户名/邮箱

3. 表格列验证:
   - 验证至少包含: ID、用户名、昵称、邮箱、手机号、角色、状态、创建时间、操作
   - 验证状态列使用 Tag 显示(启用-绿色,禁用-红色)
   - 验证操作列包含: 编辑、重置密码、删除按钮

4. 分页器验证:
   - 验证分页器显示(每页 10 条)
   - 验证总记录数正确

预期结果:
✅ 页面标题正确显示
✅ 「新增用户」按钮可点击
✅ 表格列完整,状态 Tag 清晰
✅ 分页器功能正常

---

【测试场景 2:创建用户】
验证创建用户的完整流程:

1. 点击「新增用户」按钮:
   - 验证 a-modal 弹窗弹出
   - 验证弹窗标题显示「创建用户」

2. 表单字段验证:
   - 验证用户名输入框存在(必填)
   - 验证密码输入框存在(必填,显示强度指示器)
   - 验证昵称输入框存在(必填)
   - 验证邮箱输入框存在(必填)
   - 验证手机号输入框存在
   - 验证角色选择器存在(必填,多选)

3. 填写用户信息:
   - 用户名: "testuser_e2e_001"
   - 密码: "Test@123456"
   - 昵称: "自动化测试用户"
   - 邮箱: "testuser001@ontograph.com"
   - 手机号: "13800138000"
   - 角色: 选择「普通用户」
   - 点击提交

4. 提交验证:
   - 验证 API 调用 POST /api/v1/users
   - 验证成功提示「用户创建成功」
   - 验证弹窗关闭
   - 验证用户出现在列表第一行
   - 验证状态为「启用」

5. 用户名唯一性校验:
   - 再次创建用户,输入相同用户名
   - 验证提示「用户名已存在」
   - 验证提交失败

6. 必填字段校验:
   - 不填写必填字段,点击提交
   - 验证出现校验提示
   - 验证提交失败

预期结果:
✅ 创建弹窗正常打开
✅ 表单字段完整,必填校验生效
✅ 用户名唯一性校验生效
✅ 密码强度指示器正常
✅ API 调用成功
✅ 列表刷新,新用户出现

---

【测试场景 3:编辑用户】
验证编辑用户信息的功能:

1. 选择用户:
   - 在列表中找到「testuser_e2e_001」
   - 点击「编辑」按钮
   - 验证弹窗弹出
   - 验证所有字段正确回填

2. 修改信息:
   - 修改昵称: "自动化测试用户-已修改"
   - 修改邮箱: "testuser001_updated@ontograph.com"
   - 修改手机号: "13900139000"
   - 添加角色: 勾选「数据分析师」
   - 点击保存

3. 提交验证:
   - 验证 API 调用 PUT /api/v1/users/{id}
   - 验证成功提示「用户更新成功」
   - 验证列表信息已更新

预期结果:
✅ 编辑弹窗正确回填
✅ 用户信息可修改
✅ 角色可多选
✅ API 调用正确
✅ 列表实时更新

---

【测试场景 4:角色分配】
验证用户角色分配功能:

1. 单用户角色修改:
   - 编辑用户
   - 清除所有角色
   - 选择「管理员」角色
   - 保存并验证

2. 多角色分配:
   - 编辑用户
   - 同时选择「普通用户」和「数据分析师」
   - 保存并验证
   - 验证用户具有两个角色的权限

3. 权限实时生效:
   - 用该用户登录
   - 验证新角色权限立即生效
   - 验证菜单和按钮权限正确

预期结果:
✅ 角色分配功能正常
✅ 支持多角色
✅ 权限实时生效
✅ 菜单和按钮权限正确

---

【测试场景 5:禁用/启用用户】
验证用户启用禁用功能:

1. 禁用用户:
   - 找到「testuser_e2e_001」
   - 点击状态开关或「禁用」操作
   - 验证确认对话框弹出
   - 点击确认
   - 验证 API 调用 PATCH /api/v1/users/{id}/toggle
   - 验证状态变为「禁用」(红色 Tag)

2. 验证禁用后无法登录:
   - 退出当前账号
   - 使用「testuser_e2e_001」登录
   - 验证登录失败
   - 验证提示「账号已禁用」

3. 启用用户:
   - 使用 admin 登录
   - 找到该用户,点击「启用」
   - 验证状态恢复为「启用」
   - 验证可以正常登录

预期结果:
✅ 禁用/启用切换流畅
✅ API 调用正确
✅ 禁用后无法登录
✅ 启用后恢复登录
✅ 状态 Tag 颜色正确

---

【测试场景 6:重置密码】
验证密码重置功能:

1. 重置密码:
   - 找到「testuser_e2e_001」
   - 点击「重置密码」按钮
   - 验证确认对话框弹出
   - 显示新密码: "Reset@123456"(或随机生成)
   - 点击确认

2. 验证新密码:
   - 验证 API 调用 POST /api/v1/users/{id}/reset-password
   - 验证成功提示「密码重置成功」
   - 退出登录
   - 使用新密码登录
   - 验证登录成功

3. 首次登录强制修改密码(如支持):
   - 验证提示「请修改初始密码」
   - 验证强制跳转到修改密码页面

预期结果:
✅ 密码重置功能正常
✅ 新密码可正常登录
✅ 首次登录强制修改密码(如支持)
✅ 密码重置记录日志

---

【测试场景 7:删除用户】
验证用户删除功能:

1. 删除确认:
   - 选择测试用户,点击「删除」
   - 验证 a-modal 确认对话框弹出
   - 验证显示用户信息
   - 验证警告提示:「删除后用户数据将标记为删除,确定继续?」

2. 执行删除:
   - 点击「确认」按钮
   - 验证 API 调用 DELETE /api/v1/users/{id}
   - 验证成功提示「用户删除成功」
   - 验证用户从列表消失(或标记为删除)

3. 逻辑删除验证:
   - 查询数据库,验证 deleted=true
   - 验证用户数据未物理删除
   - 验证关联的角色关系已清理

4. 取消删除:
   - 选择另一用户,点击删除
   - 点击「取消」按钮
   - 验证对话框关闭
   - 验证用户仍在列表中

预期结果:
✅ 删除确认对话框显示完整
✅ 逻辑删除(deleted=true)
✅ 关联关系清理
✅ 数据未物理删除
✅ 取消删除不生效

---

【测试场景 8:用户搜索】
验证用户搜索和过滤功能:

1. 用户名搜索:
   - 在搜索框输入「testuser」
   - 验证列表过滤出匹配的用户
   - 验证搜索结果准确

2. 邮箱搜索:
   - 输入邮箱关键词「ontograph」
   - 验证返回匹配的邮箱

3. 状态过滤:
   - 选择状态过滤:「启用」
   - 验证只显示启用的用户
   - 选择「禁用」,验证只显示禁用的用户

4. 清空搜索:
   - 清空搜索框
   - 验证列表恢复到初始状态

预期结果:
✅ 用户名搜索准确
✅ 邮箱搜索有效
✅ 状态过滤正确
✅ 清空搜索恢复列表

---

【API 端点对照】

| 操作 | API 方法 | 路径 | 说明 |
|------|---------|------|------|
| 获取用户列表 | GET | /api/v1/users?page=&size=&keyword= | 返回用户数组,支持分页和搜索 |
| 创建用户 | POST | /api/v1/users | 创建用户,返回 userId |
| 更新用户 | PUT | /api/v1/users/{id} | 更新用户信息 |
| 删除用户 | DELETE | /api/v1/users/{id} | 逻辑删除用户 |
| 切换启用状态 | PATCH | /api/v1/users/{id}/toggle | 切换 enabled 状态 |
| 重置密码 | POST | /api/v1/users/{id}/reset-password | 重置用户密码 |
| 获取角色列表 | GET | /api/v1/roles | 获取可选角色列表 |

---

【问题诊断】

| 浏览器现象 | 可能原因 | 检查文件 | 修复建议 |
|-----------|---------|---------|---------|
| 用户列表不加载 | getUsers() API 失败 | `UserManagement.vue:行号` | 检查后端服务,API 路径 |
| 创建用户失败 | 用户名重复校验 | 后端唯一约束 | 使用唯一用户名 |
| 密码强度校验失败 | 密码不符合规则 | `UserForm.vue:行号` | 检查密码规则配置 |
| 角色选择器空白 | getRoles() 返回空 | `RoleSelector.vue:行号` | 检查角色数据 |
| 禁用后仍可登录 | toggle API 未生效 | 后端状态更新逻辑 | 检查 enabled 字段 |
| 重置密码失败 | reset-password API 错误 | `UserManagement.vue:行号` | 检查 POST 请求 |
| 删除用户后仍显示 | 列表未刷新 | `UserManagement.vue:行号` | 验证 delete 后是否 fetch |
| 搜索不生效 | keyword 参数未传递 | `UserManagement.vue:行号` | 检查搜索逻辑 |

---

【关键验证点】
⚠️ 创建用户必须校验用户名唯一性
⚠️ 密码必须符合强度规则
⚠️ 角色分配必须支持多选
⚠️ 禁用用户后必须无法登录
⚠️ 删除必须是逻辑删除(deleted=true)
⚠️ 重置密码后必须可正常登录
⚠️ 搜索和过滤必须准确

【测试执行要求】
1. 按顺序执行所有测试场景
2. 每个场景完成后截图保存
3. 记录每个场景的测试结果(PASS/FAIL)
4. 验证 API 调用参数和响应
5. 特别注意禁用/启用和重置密码的登录验证
6. 验证删除为逻辑删除,非物理删除
7. 测试完成后清理测试用户数据
```

---

##### 开发提示词

> ❌ **缺失模块**。**开发任务**:创建 `src/views/system/UserManagement.vue` + 子组件 `UserForm.vue`、`UserTable.vue`、`RoleSelector.vue` + `src/api/user.ts`。**核心特性**:(1) 用户 CRUD,角色分配;(2) 用户启用/禁用,逻辑删除;(3) 密码重置,密码强度校验;(4) 用户列表展示与搜索,分页;(5) 角色多选,权限实时生效;(6) 用户名唯一性校验;(7) 回归测试 4.5.1 全部 8 个场景。

---

##### 关键 API 取证

| 方法 | 路径 | 用途 |
|------|------|------|
| GET | `/api/v1/users` | 获取用户列表(含分页、搜索) |
| POST | `/api/v1/users` | 创建用户 |
| PUT | `/api/v1/users/{id}` | 更新用户 |
| DELETE | `/api/v1/users/{id}` | 删除用户(逻辑删除) |
| PATCH | `/api/v1/users/{id}/toggle` | 切换启用状态 |
| POST | `/api/v1/users/{id}/reset-password` | 重置密码 |
| GET | `/api/v1/roles` | 获取角色列表 |

---

---

## 4.5.2 角色管理 - 补充内容

```markdown
##### Skill 配置

**引用的 Skill**:
- `browse` - 页面加载验证
- `qa` - 完整功能测试

**前置条件**:
- ✅ 后端服务运行中
- ✅ 前端服务运行中
- ✅ 用户已登录 (admin)
- ✅ 存在至少 1 个测试角色
- ✅ 存在菜单数据

##### Skill 提示词: 角色管理测试

**页面路径**: `/system/role`  
**源码文件**: `src/views/system/RoleManagement.vue`  
**组件依赖**: `@/components/role/RoleForm.vue`, `MenuPermission.vue`  
**API 文件**: `@/api/role.ts` - `getRoles()`, `createRole()`, `updateRole()`, `deleteRole()`, `updatePermissions()`

```prompt
/browser 或 /open-gstack-browser
打开 OntoGraph 系统角色管理页面,执行完整的角色管理功能测试。

【前置操作】
1. 使用 admin/admin123 登录系统
2. 导航到 http://localhost:5173/system/role
3. 等待页面加载完成

---

【测试场景 1:页面加载与角色列表】
验证角色管理页面的基础渲染:

1. 页面标题验证:「角色管理」
2. 「新增角色」按钮存在
3. 角色列表表格显示:
   - 列: ID、角色名称、角色编码、描述、用户数、状态、创建时间、操作
   - 操作列: 编辑、删除
4. 分页器正常

预期结果:
✅ 页面标题正确
✅ 角色列表完整
✅ 用户数统计准确

---

【测试场景 2:创建角色】
验证创建角色的完整流程:

1. 点击「新增角色」
2. 填写表单:
   - 角色名称:「测试角色-数据分析师」
   - 角色编码:「test_data_analyst」
   - 描述:「具有数据查看和分析权限」
3. 分配菜单权限:
   - ✅ 图谱列表
   - ✅ 图谱 IDE
   - ✅ 数据导入
   - ❌ 用户管理
4. 保存并验证:
   - API: POST /api/v1/roles
   - 角色创建成功
   - 权限分配正确

预期结果:
✅ 角色创建成功
✅ 菜单权限树正常
✅ 权限保存成功

---

【测试场景 3:编辑角色权限】
验证编辑角色和权限更新:

1. 编辑「测试角色-数据分析师」
2. 新增权限:
   - ✅ 数据导出
   - ✅ 本体管理
3. 保存并验证:
   - API: PUT /api/v1/roles/{id}
   - 权限更新成功
4. 用该角色用户登录:
   - 验证新权限生效
   - 菜单和按钮正确

预期结果:
✅ 权限更新成功
✅ 用户权限实时生效

---

【测试场景 4:删除角色】
验证角色删除功能:

1. 选择测试角色,点击删除
2. 验证确认对话框
3. 检查关联用户:
   - 如果有用户关联,提示先移除用户
   - 如果无关联,允许删除
4. 执行删除:
   - API: DELETE /api/v1/roles/{id}
   - 删除成功

预期结果:
✅ 删除前检查关联
✅ 有关联时阻止删除
✅ 无关联时删除成功

---

【API 端点对照】

| 操作 | API 方法 | 路径 | 说明 |
|------|---------|------|------|
| 获取角色列表 | GET | /api/v1/roles | 返回角色数组 |
| 创建角色 | POST | /api/v1/roles | 创建角色 |
| 更新角色 | PUT | /api/v1/roles/{id} | 更新角色信息 |
| 删除角色 | DELETE | /api/v1/roles/{id} | 删除角色 |
| 更新角色权限 | PUT | /api/v1/roles/{id}/permissions | 更新菜单权限 |
| 获取菜单树 | GET | /api/v1/menus | 获取菜单树(权限分配用) |

---

【问题诊断】

| 浏览器现象 | 可能原因 | 检查文件 | 修复建议 |
|-----------|---------|---------|---------|
| 角色列表不加载 | getRoles() API 失败 | `RoleManagement.vue` | 检查后端服务 |
| 创建角色失败 | 角色编码重复 | 后端唯一约束 | 使用唯一编码 |
| 菜单树不显示 | getMenus() 失败 | `MenuPermission.vue` | 检查菜单 API |
| 权限保存失败 | permissions API 错误 | `RoleManagement.vue` | 检查 PUT 请求 |
| 删除角色失败 | 存在关联用户 | 后端关联检查 | 先移除用户 |

---

【关键验证点】
⚠️ 角色编码必须全局唯一
⚠️ 菜单权限树必须正确显示
⚠️ 权限更新必须实时生效
⚠️ 删除前必须检查关联用户
⚠️ 权限保存必须包含完整的菜单ID列表

【测试执行要求】
1. 按顺序执行所有测试场景
2. 验证权限实时生效
3. 测试完成后清理测试角色
```

---

##### 开发提示词

> ❌ **缺失模块**。**开发任务**:创建 `src/views/system/RoleManagement.vue` + 子组件 `RoleForm.vue`、`MenuPermission.vue` + `src/api/role.ts`。**核心特性**:(1) 角色 CRUD,编码唯一;(2) 菜单权限分配,树形选择;(3) 角色列表展示,用户数统计;(4) 权限实时生效;(5) 删除前关联检查;(6) 回归测试 4.5.2 全部 4 个场景。

---

##### 关键 API 取证

| 方法 | 路径 | 用途 |
|------|------|------|
| GET | `/api/v1/roles` | 获取角色列表 |
| POST | `/api/v1/roles` | 创建角色 |
| PUT | `/api/v1/roles/{id}` | 更新角色 |
| DELETE | `/api/v1/roles/{id}` | 删除角色 |
| PUT | `/api/v1/roles/{id}/permissions` | 更新角色权限 |
| GET | `/api/v1/menus` | 获取菜单树 |

---

---

## 4.5.3 菜单管理 - 补充内容

```markdown
##### Skill 配置

**引用的 Skill**:
- `browse` - 页面加载验证
- `qa` - 完整功能测试

**前置条件**:
- ✅ 后端服务运行中
- ✅ 前端服务运行中
- ✅ 用户已登录 (admin)
- ✅ 存在菜单数据

##### Skill 提示词: 菜单管理测试

**页面路径**: `/system/menu`  
**源码文件**: `src/views/system/MenuManagement.vue`  
**组件依赖**: `@/components/menu/MenuTree.vue`, `MenuForm.vue`  
**API 文件**: `@/api/menu.ts` - `getMenus()`, `createMenu()`, `updateMenu()`, `deleteMenu()`

```prompt
/browser 或 /open-gstack-browser
打开 OntoGraph 系统菜单管理页面,执行完整的菜单管理功能测试。

【前置操作】
1. 使用 admin/admin123 登录系统
2. 导航到 http://localhost:5173/system/menu
3. 等待页面加载完成

---

【测试场景 1:页面加载与菜单树】
验证菜单树的基础渲染:

1. 页面标题:「菜单管理」
2. 「新增菜单」按钮存在
3. 菜单树正确显示:
   - 层级关系清晰
   - 图标、名称、排序、权限标识
   - 可展开/折叠
4. 操作按钮: 编辑、删除

预期结果:
✅ 菜单树层级正确
✅ 图标和名称显示
✅ 排序顺序正确

---

【测试场景 2:新增菜单】
验证创建菜单的完整流程:

1. 点击「新增菜单」
2. 填写表单:
   - 菜单名称:「测试菜单」
   - 父菜单:选择「数据管理」
   - 路由路径:「/data/test」
   - 权限标识:「data:test:view」
   - 排序:99
   - 图标:选择图标
3. 保存并验证:
   - API: POST /api/v1/menus
   - 菜单创建成功
   - 菜单树更新

预期结果:
✅ 菜单创建成功
✅ 父子关系正确
✅ 菜单树更新

---

【测试场景 3:编辑菜单】
验证编辑菜单功能:

1. 选择测试菜单,点击编辑
2. 修改名称、排序、权限标识
3. 保存并验证:
   - API: PUT /api/v1/menus/{id}
   - 菜单更新成功
   - 菜单树刷新

预期结果:
✅ 编辑功能正常
✅ 信息更新成功

---

【测试场景 4:删除菜单】
验证菜单删除功能:

1. 选择测试菜单,点击删除
2. 检查子菜单:
   - 如果有子菜单,提示先删除子菜单
   - 如果无子菜单,允许删除
3. 执行删除:
   - API: DELETE /api/v1/menus/{id}
   - 删除成功

预期结果:
✅ 删除前检查子菜单
✅ 有子菜单时阻止删除
✅ 无子菜单时删除成功

---

【API 端点对照】

| 操作 | API 方法 | 路径 | 说明 |
|------|---------|------|------|
| 获取菜单树 | GET | /api/v1/menus | 返回菜单树(递归结构) |
| 创建菜单 | POST | /api/v1/menus | 创建菜单 |
| 更新菜单 | PUT | /api/v1/menus/{id} | 更新菜单 |
| 删除菜单 | DELETE | /api/v1/menus/{id} | 删除菜单 |

---

【问题诊断】

| 浏览器现象 | 可能原因 | 检查文件 | 修复建议 |
|-----------|---------|---------|---------|
| 菜单树不显示 | getMenus() 失败 | `MenuManagement.vue` | 检查后端服务 |
| 创建菜单失败 | 父菜单ID无效 | 后端校验 | 检查 parentId |
| 删除菜单失败 | 存在子菜单 | 后端关联检查 | 先删除子菜单 |

---

【关键验证点】
⚠️ 菜单树必须正确显示层级关系
⚠️ 父子关系必须正确设置
⚠️ 删除前必须检查子菜单
⚠️ 权限标识符必须唯一

【测试执行要求】
1. 按顺序执行所有测试场景
2. 验证菜单树层级正确
3. 测试完成后清理测试菜单
```

---

##### 开发提示词

> ❌ **缺失模块**。**开发任务**:创建 `src/views/system/MenuManagement.vue` + 子组件 `MenuTree.vue`、`MenuForm.vue` + `src/api/menu.ts`。**核心特性**:(1) 菜单树展示,父子关系;(2) 菜单 CRUD,权限标识;(3) 菜单排序;(4) 删除前子菜单检查;(5) 图标选择器;(6) 回归测试 4.5.3 全部 4 个场景。

---

##### 关键 API 取证

| 方法 | 路径 | 用途 |
|------|------|------|
| GET | `/api/v1/menus` | 获取菜单树 |
| POST | `/api/v1/menus` | 创建菜单 |
| PUT | `/api/v1/menus/{id}` | 更新菜单 |
| DELETE | `/api/v1/menus/{id}` | 删除菜单 |

---

---

## 4.5.4 系统配置 - 补充内容

```markdown
##### Skill 配置

**引用的 Skill**:
- `browse` - 页面加载验证
- `qa` - 完整功能测试

**前置条件**:
- ✅ 后端服务运行中
- ✅ 前端服务运行中
- ✅ 用户已登录 (admin)
- ✅ 存在配置数据

##### Skill 提示词: 系统配置测试

**页面路径**: `/system/config`  
**源码文件**: `src/views/system/ConfigManagement.vue`  
**组件依赖**: `@/components/config/ConfigTable.vue`, `ConfigForm.vue`  
**API 文件**: `@/api/config.ts` - `getConfigs()`, `updateConfig()`, `getConfigGroups()`

```prompt
/browser 或 /open-gstack-browser
打开 OntoGraph 系统配置管理页面,执行完整的系统配置功能测试。

【前置操作】
1. 使用 admin/admin123 登录系统
2. 导航到 http://localhost:5173/system/config
3. 等待页面加载完成

---

【测试场景 1:页面加载与配置列表】
验证配置列表的基础渲染:

1. 页面标题:「系统配置」
2. 配置分组筛选下拉框存在
3. 配置列表表格显示:
   - 列: ID、配置键、配置名称、配置值、类型、分组、描述、操作
   - 操作列: 编辑
4. 分页器正常

预期结果:
✅ 配置列表完整
✅ 分组筛选可用
✅ 配置值清晰显示

---

【测试场景 2:修改配置】
验证配置修改功能:

1. 找到「搜索缓存过期时间」配置
2. 点击「编辑」
3. 修改值为:1800 (30 分钟)
4. 保存并验证:
   - API: PUT /api/v1/configs/{key}
   - 配置更新成功
5. 重启服务,验证配置持久化

预期结果:
✅ 配置修改成功
✅ 配置持久化
✅ 重启后配置保留

---

【测试场景 3:配置分组筛选】
验证配置分组筛选功能:

1. 选择分组:「搜索配置」
2. 验证只显示该分组的配置
3. 选择「系统配置」
4. 验证只显示系统配置
5. 清空筛选,显示所有配置

预期结果:
✅ 分组筛选准确
✅ 各分组配置正确过滤

---

【API 端点对照】

| 操作 | API 方法 | 路径 | 说明 |
|------|---------|------|------|
| 获取配置列表 | GET | /api/v1/configs?group= | 返回配置数组,支持分组筛选 |
| 更新配置 | PUT | /api/v1/configs/{key} | 更新配置值 |
| 获取配置分组 | GET | /api/v1/configs/groups | 返回配置分组列表 |

---

【问题诊断】

| 浏览器现象 | 可能原因 | 检查文件 | 修复建议 |
|-----------|---------|---------|---------|
| 配置列表不加载 | getConfigs() API 失败 | `ConfigManagement.vue` | 检查后端服务 |
| 配置修改失败 | 配置值类型错误 | 后端校验 | 检查值类型 |
| 配置未持久化 | 数据库写入失败 | 后端逻辑 | 检查数据库 |

---

【关键验证点】
⚠️ 配置修改必须持久化到数据库
⚠️ 重启后配置必须保留
⚠️ 配置值类型必须正确校验
⚠️ 分组筛选必须准确

【测试执行要求】
1. 按顺序执行所有测试场景
2. 验证配置持久化
3. 不要修改关键系统配置
```

---

##### 开发提示词

> ❌ **缺失模块**。**开发任务**:创建 `src/views/system/ConfigManagement.vue` + 子组件 `ConfigTable.vue`、`ConfigForm.vue` + `src/api/config.ts`。**核心特性**:(1) 配置列表展示,分组显示;(2) 配置修改,多种类型(string/number/boolean);(3) 配置持久化,数据库存储;(4) 配置分组筛选;(5) 配置搜索;(6) 回归测试 4.5.4 全部 3 个场景。

---

##### 关键 API 取证

| 方法 | 路径 | 用途 |
|------|------|------|
| GET | `/api/v1/configs` | 获取配置列表(含分组筛选) |
| PUT | `/api/v1/configs/{key}` | 更新配置 |
| GET | `/api/v1/configs/groups` | 获取配置分组 |

---

---

## 4.5.5 操作日志 - 补充内容

```markdown
##### Skill 配置

**引用的 Skill**:
- `browse` - 页面加载验证
- `qa` - 完整功能测试

**前置条件**:
- ✅ 后端服务运行中
- ✅ 前端服务运行中
- ✅ 用户已登录 (admin)
- ✅ 存在操作日志数据

##### Skill 提示词: 操作日志测试

**页面路径**: `/system/log`  
**源码文件**: `src/views/system/OperationLog.vue`  
**组件依赖**: `@/components/log/LogTable.vue`, `LogFilter.vue`, `LogDetail.vue`  
**API 文件**: `@/api/log.ts` - `getLogs()`, `getLogDetail()`, `getLogTypes()`

```prompt
/browser 或 /open-gstack-browser
打开 OntoGraph 系统操作日志页面,执行完整的日志查询功能测试。

【前置操作】
1. 使用 admin/admin123 登录系统
2. 导航到 http://localhost:5173/system/log
3. 等待页面加载完成

---

【测试场景 1:页面加载与日志列表】
验证日志列表的基础渲染:

1. 页面标题:「操作日志」
2. 日志列表表格显示:
   - 列: ID、操作用户、操作时间、操作类型、操作内容、IP地址、结果、操作
   - 操作列: 查看详情
3. 筛选区:
   - 时间范围选择
   - 用户选择
   - 操作类型选择
4. 分页器正常

预期结果:
✅ 日志列表完整
✅ 筛选区功能完整
✅ 分页器正常

---

【测试场景 2:日志自动记录验证】
验证操作日志自动记录:

1. 前置操作:创建一个用户
2. 导航到操作日志页面
3. 筛选操作类型:「创建用户」
4. 验证日志记录包含:
   - 操作用户:admin
   - 操作时间:当前时间
   - 操作内容:创建用户 xxx
   - 请求方法:POST /api/v1/users
   - IP地址:127.0.0.1
   - 执行结果:成功

预期结果:
✅ 日志记录完整
✅ 敏感信息已脱敏
✅ 时间戳准确

---

【测试场景 3:日志筛选】
验证日志筛选功能:

1. 按时间范围筛选
2. 按用户筛选
3. 按操作类型筛选
4. 组合筛选条件
5. 清空筛选

预期结果:
✅ 筛选功能准确
✅ 组合筛选有效
✅ 清空筛选恢复列表

---

【测试场景 4:日志详情】
验证日志详情查看:

1. 选择日志记录,点击「查看详情」
2. 验证详情面板显示:
   - 完整操作信息
   - 请求参数(脱敏)
   - 响应结果
   - 执行时长
   - 错误信息(如有)

预期结果:
✅ 详情信息完整
✅ 敏感信息脱敏
✅ 执行时长准确

---

【API 端点对照】

| 操作 | API 方法 | 路径 | 说明 |
|------|---------|------|------|
| 获取日志列表 | GET | /api/v1/logs?page=&size=&type=&userId= | 返回日志数组,支持筛选 |
| 获取日志详情 | GET | /api/v1/logs/{id} | 返回日志详细信息 |
| 获取操作类型 | GET | /api/v1/logs/types | 返回操作类型列表 |

---

【问题诊断】

| 浏览器现象 | 可能原因 | 检查文件 | 修复建议 |
|-----------|---------|---------|---------|
| 日志列表不加载 | getLogs() API 失败 | `OperationLog.vue` | 检查后端服务 |
| 日志未记录 | AOP切面未生效 | 后端日志配置 | 检查切面配置 |
| 详情空白 | getLogDetail() 失败 | `LogDetail.vue` | 检查 API |

---

【关键验证点】
⚠️ 操作日志必须自动记录
⚠️ 敏感信息必须脱敏(密码等)
⚠️ 筛选功能必须准确
⚠️ 日志详情必须完整

【测试执行要求】
1. 按顺序执行所有测试场景
2. 验证日志自动记录
3. 验证敏感信息脱敏
```

---

##### 开发提示词

> ❌ **缺失模块**。**开发任务**:创建 `src/views/system/OperationLog.vue` + 子组件 `LogTable.vue`、`LogFilter.vue`、`LogDetail.vue` + `src/api/log.ts`。**核心特性**:(1) 操作日志自动记录,AOP切面;(2) 日志列表展示,分页;(3) 日志筛选,时间+用户+操作类型;(4) 日志详情查看,敏感信息脱敏;(5) 操作类型枚举;(6) 回归测试 4.5.5 全部 4 个场景。

---

##### 关键 API 取证

| 方法 | 路径 | 用途 |
|------|------|------|
| GET | `/api/v1/logs` | 获取日志列表(含筛选) |
| GET | `/api/v1/logs/{id}` | 获取日志详情 |
| GET | `/api/v1/logs/types` | 获取操作类型列表 |

---

---

## 4.5.6 系统监控 - 补充内容

```markdown
##### Skill 配置

**引用的 Skill**:
- `browse` - 页面加载验证
- `qa` - 完整功能测试

**前置条件**:
- ✅ 后端服务运行中
- ✅ 前端服务运行中
- ✅ 用户已登录 (admin)
- ✅ PostgreSQL/Neo4j/Redis 服务运行中

##### Skill 提示词: 系统监控测试

**页面路径**: `/monitor`  
**源码文件**: `src/views/monitor/SystemMonitor.vue`  
**组件依赖**: `@/components/monitor/HealthCheck.vue`, `PerformanceMetrics.vue`  
**API 文件**: `@/api/monitor.ts` - `getHealth()`, `getMetrics()`, `getServices()`

```prompt
/browser 或 /open-gstack-browser
打开 OntoGraph 系统监控页面,执行完整的系统监控功能测试。

【前置操作】
1. 使用 admin/admin123 登录系统
2. 导航到 http://localhost:5173/monitor
3. 等待页面加载完成

---

【测试场景 1:页面加载与健康状态】
验证系统健康状态显示:

1. 页面标题:「系统监控」
2. 健康状态卡片显示:
   - 后端服务状态: UP(绿色)
   - PostgreSQL 连接: UP(绿色)
   - Neo4j 连接: UP(绿色)
   - Redis 连接: UP(绿色)
3. 整体状态: UP

预期结果:
✅ 所有服务状态正常
✅ 连接状态正确
✅ 健康检查实时

---

【测试场景 2:性能指标】
验证性能指标显示:

1. CPU 使用率:
   - 百分比显示
   - 进度条可视化
2. 内存使用情况:
   - 已用/总量
   - 使用率百分比
3. JVM 信息:
   - JVM 版本
   - 堆内存使用
   - 非堆内存使用
4. 数据库连接池:
   - 活跃连接数
   - 空闲连接数
   - 最大连接数

预期结果:
✅ 性能指标显示完整
✅ 数据实时更新
✅ 指标准确

---

【测试场景 3:API 健康验证】
验证健康检查 API:

1. 访问: http://localhost:9090/actuator/health
2. 验证返回:
   ```json
   {
     "status": "UP"
   }
   ```
3. 验证各组件状态:
   - db: UP
   - neo4j: UP
   - redis: UP

预期结果:
✅ 健康检查 API 正常
✅ 状态为 UP
✅ 各组件状态正确

---

【测试场景 4:实时数据刷新】
验证监控数据实时刷新:

1. 观察数据自动刷新
2. 验证刷新频率(每 5-10 秒)
3. 手动刷新按钮(如有)
4. 验证数据变化准确

预期结果:
✅ 数据自动刷新
✅ 刷新频率合理
✅ 数据变化准确

---

【API 端点对照】

| 操作 | API 方法 | 路径 | 说明 |
|------|---------|------|------|
| 获取健康状态 | GET | /api/v1/monitor/health | 返回系统健康状态 |
| 获取性能指标 | GET | /api/v1/monitor/metrics | 返回 CPU/内存等指标 |
| 获取服务状态 | GET | /api/v1/monitor/services | 返回各服务状态 |
| Actuator 健康检查 | GET | /actuator/health | Spring Boot Actuator |

---

【问题诊断】

| 浏览器现象 | 可能原因 | 检查文件 | 修复建议 |
|-----------|---------|---------|---------|
| 健康状态显示 DOWN | 服务未启动 | 后端日志 | 检查服务状态 |
| 性能指标空白 | getMetrics() 失败 | `SystemMonitor.vue` | 检查 API |
| 数据不刷新 | 定时器未启动 | `SystemMonitor.vue` | 检查 setInterval |

---

【关键验证点】
⚠️ 所有服务状态必须正确显示
⚠️ 性能指标必须实时更新
⚠️ Actuator 健康检查必须正常
⚠️ 数据刷新频率合理

【测试执行要求】
1. 按顺序执行所有测试场景
2. 验证所有服务状态
3. 确保 PostgreSQL/Neo4j/Redis 运行中
```

---

##### 开发提示词

> ❌ **缺失模块**。**开发任务**:创建 `src/views/monitor/SystemMonitor.vue` + 子组件 `HealthCheck.vue`、`PerformanceMetrics.vue` + `src/api/monitor.ts`。**核心特性**:(1) 系统健康检查,服务状态监控;(2) 性能指标,CPU/内存/磁盘;(3) 数据库连接状态(PostgreSQL/Neo4j/Redis);(4) API 健康验证,Actuator集成;(5) 实时数据刷新;(6) 回归测试 4.5.6 全部 4 个场景。

---

##### 关键 API 取证

| 方法 | 路径 | 用途 |
|------|------|------|
| GET | `/api/v1/monitor/health` | 获取系统健康状态 |
| GET | `/api/v1/monitor/metrics` | 获取性能指标 |
| GET | `/api/v1/monitor/services` | 获取服务状态 |
| GET | `/actuator/health` | Spring Boot Actuator 健康检查 |

---

---

