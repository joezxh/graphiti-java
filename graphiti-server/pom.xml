<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.graphiti</groupId>
        <artifactId>graphiti-java</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>
    <artifactId>graphiti-server</artifactId>
    <name>Graphiti Server</name>
    <description>Graphiti 知识图谱后端服务启动模块</description>
    <packaging>jar</packaging>
    
    <dependencies>
        <!-- 系统模块 -->
        <dependency>
            <groupId>com.graphiti</groupId>
            <artifactId>graphiti-module-system</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- 核心业务模块 -->
        <dependency>
            <groupId>com.graphiti</groupId>
            <artifactId>graphiti-module-core</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- Web Starter -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <!-- SpringDoc API 文档 -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>${spring.boot.version}</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <encoding>${project.build.sourceEncoding}</encoding>
                    <compilerArgs>
                        <arg>-parameters</arg>
                    </compilerArgs>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                        <path>
                            <groupId>org.mapstruct</groupId>
                            <artifactId>mapstruct-processor</artifactId>
                            <version>${mapstruct.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <!-- 前端构建插件: 编译 Vue 前端并嵌入 Spring Boot -->
            <!-- 前端构建已由 Dockerfile 处理，不再需要 frontend-maven-plugin -->
        </plugins>
    </build>
</project>

